# Benchmarks

Benchmark results from private drawings must remain under `benchmarks/results/`,
which is ignored by Git.

The converter JSON report contains timing, peak RSS where the operating system
supports it, entity counts, unknown entity names and text-integrity counters.
