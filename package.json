{
  "name": "dwg-viewer",
  "version": "0.1.0",
  "private": true,
  "description": "Free and open-source DWG viewer for VS Code",
  "license": "MPL-2.0",
  "packageManager": "pnpm@11.12.0",
  "scripts": {
    "check": "cargo fmt --all -- --check && cargo clippy --workspace --all-targets -- -D warnings && cargo test --workspace && pnpm --filter @dwg-viewer/webview check && pnpm --filter dwg-viewer-vscode check",
    "format": "cargo fmt --all",
    "test": "cargo test --workspace && pnpm --filter @dwg-viewer/webview test && pnpm --filter dwg-viewer-vscode test",
    "test:webview": "pnpm --filter @dwg-viewer/webview test",
    "test:vscode": "pnpm --filter dwg-viewer-vscode test"
  }
}
