{
  "name": "@dwg-viewer/render-protocol",
  "version": "0.1.0",
  "description": "Versioned source, snapshot, delta, identity, and range contracts for DWG Viewer Core",
  "license": "MPL-2.0",
  "private": true,
  "type": "module",
  "exports": "./src/index.mjs",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "check": "node --check src/*.mjs && node --test test/*.test.mjs",
    "test": "node --test test/*.test.mjs"
  }
}
